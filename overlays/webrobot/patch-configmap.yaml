apiVersion: v1
kind: ConfigMap
metadata:
  name: postiz-config
data:
  MAIN_URL: "https://postiz.webrobot.eu"
  FRONTEND_URL: "https://postiz.webrobot.eu"
  NEXT_PUBLIC_BACKEND_URL: "https://postiz.webrobot.eu/api"
  # REDIS_URL (host + password for the existing nuvolaris Redis) is set in the
  # gitignored secret.yaml — kept out of this published file.
  # Signups locked (admin ceo@webrobot.eu already created).
  DISABLE_REGISTRATION: "true"
